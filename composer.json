{
    "name": "oroshi/security-interop",
    "type": "library",
    "description": "Authentication, authorization, & access control interop components for Oroshi projects.",
    "keywords": [ "oroshi", "authentication", "authorization", "security", "acl", "interop" ],
    "homepage": "https://github.com/oroshi/security-interop",
    "license": "MPL-2.0",
    "prefer-stable": true,
    "minimum-stability": "dev",
    "require": {
        "php": "^7.4",
        "beberlei/assert": "^v3.0",
        "firebase/php-jwt": "^v5.0",
        "oroshi/oroshi-core": "master@dev",
        "zendframework/zend-permissions-acl": "^2.7"
    },
    "require-dev": {
        "phpunit/phpunit": "^8.0",
        "sebastian/phpcpd": "^4.1",
        "squizlabs/php_codesniffer": "^3.3",
        "vimeo/psalm": "^3.0"
    },
    "autoload": {
        "psr-4": { "Oroshi\\Security\\" : "src" }
    },
    "autoload-dev": {
        "psr-4": { "Oroshi\\Tests\\Security\\": "tests" }
    },
    "archive": {
        "exclude": [
            "/build",
            "/vendor"
        ]
    },
    "scripts": {
        "build": [ "@sniff", "@tests" ],
        "code-sniffer-autofix": "phpcbf --standard=PSR2 -d tabWidth=4 src tests",
        "code-sniffer": "phpcs -p -s --extensions=php --standard='PSR1,PSR2' src tests",
        "copy-paste-detector": "phpcpd src tests || true",
        "sniff": [
            "@code-sniffer",
            "@copy-paste-detector",
            "@validate-json",
            "psalm"
        ],
        "test": "phpunit --no-coverage",
        "tests": "phpunit",
        "validate-deps": "composer validate --with-dependencies --strict",
        "validate-json": "composer validate --no-check-all --no-check-lock"
    }
}
